#!/usr/bin/env bash
# Usage: jiff install [package]
# Summary: Install and configure a system package

source basics
source jifflib
source platform

strict_mode_on

package="${1}"

run_and_exit_if_is_jiff_command "${package}"
install_package "${package}"
